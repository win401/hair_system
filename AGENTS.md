<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Claude ↔ Codex 협업 노트

이 파일은 Claude와 Codex가 이 프로젝트(AI 헤어스타일 시뮬레이션 서비스)에 대해
서로 의견을 남기고 종합하기 위한 공용 노트다. 사람이 두 에이전트에게 같은
질문을 각각 던지고 여기서 답을 맞춰보는 용도로 쓴다.

기획 문서 본체는 `docs/` 폴더에 있다. 작업 전에 `docs/README.md`부터 읽을 것.
MVP 구현 계획은 `docs/05-mvp-implementation-plan.md`에 있다.

## 사용 규칙

1. 새 의견/질문/답변은 맨 아래 "에이전트 로그"에 **새 항목을 추가**한다. 기존
   항목은 수정하거나 지우지 않는다 (사람이 대화 흐름을 추적해야 함).
2. 항목 형식:
   ```
   ### YYYY-MM-DD - {Claude|Codex}
   {내용}
   ```
3. 합의되었거나 사람이 확정한 사항은 "확정 사항"으로 옮긴다.
4. 아직 답이 안 나온 질문은 "열린 질문"에 추가하고, 답이 나오면 지운다.
5. 실제 현장 정보(사촌 미용실, 사촌 부모님 미용실 피드백 등)는 "현장 피드백"
   섹션에 날짜와 출처를 남긴다 — 이게 이 프로젝트의 가장 값진 데이터다.

## 프로젝트 한 줄 요약

셀카로 헤어스타일을 미리 체험 → 현재 모발 기준 시술 가능성 검토 → 전문
디자이너 상담 카드 → 네이버 예약으로 연결하는 서비스. 네이버는 검색/예약/결제를
그대로 담당하고, 우리는 "스타일 결정 + 디자이너 매칭" 구간만 담당한다.

## 현장 피드백

<!-- 예: ### 2026-09-20 - 사촌 (바버샵 운영) - 사용자 전달
내용...
-->

### 2026-09-16 - 사용자 제공 배경 정보

- 사용자는 이 서비스를 1인 사업으로 진행할 계획이다.
- 사촌, 이모, 이모부가 모두 미용 또는 이용업에 종사하고 있어 현장 인터뷰,
  초기 피드백, 파일럿 운영과 제한적인 홍보에 도움을 받을 가능성이 있다.
- 지인인 투탁핀셔님에게 콘텐츠 또는 홍보 관련 도움을 받을 가능성이 있다.
- 아직 각 인물의 구체적인 참여 의사, 역할, 전문 분야, 지역, 고객층은 확인되지
  않았다.

### 2026-09-16 - 설문 응답자 1명 (Google 설문, 익명)

- 역할: 미용실 원장·대표, 현장 경력 4~7년.
- 주 담당: 남성 커트·남성 펌·두피/헤어 케어. 예약·문의 채널은 네이버 예약이며,
  매장 운영 초기라 현 운영 방식은 아직 정착되지 않은 상태라고 응답했다.
- 고객 참고 사진은 가끔 가져오며, 현재는 참고 사진 상담에 특별한 문제가 없다고
  응답했다. 따라서 이 한 응답만으로 "상담 문제 해결" 수요를 확정할 수 없다.
- 예약 전 상담 카드가 도움이 되는 정도는 5점(매우 도움)으로 평가했다. 카드에는
  원하는 스타일 사진, 현재 머리 정면·측면 사진, 현재 길이, 최근 펌·염색·탈색
  이력, 예상 시술 시간·가격대를 우선 포함해야 한다고 골랐다.
- AI 체험의 우려로 실제 불가능한 결과, 얼굴 변화로 인한 기대 상승, 결과 설명·수정
  업무 증가를 선택했고, 직접 사용 전에는 모르겠다는 답도 함께 선택했다.
- 관리 방식과 가격은 아직 판단하기 어렵다고 답했다. 초기 테스트에는 포트폴리오
  사진 제공, AI 결과의 시술 가능성 검토, 네이버 예약 연결 테스트가 가능하다고
  골랐으나 후속 연락은 "나중에 다시"를 요청했다.
- 자유 의견: "아직은 적용되는데 시간이 걸릴거같음". 오픈 초기의 운영 부담을
  반영한 신호로 해석하며, 즉시 도입을 밀기보다 2~4주 뒤 짧은 재접촉이 적절하다.

## 확정 사항

- 사업은 고정 개발팀을 전제로 하지 않고 사용자 1인이 주도한다.
- 가족 업계 네트워크를 첫 현장 인터뷰와 비공개 파일럿 후보로 활용한다.
- 가족의 반응만으로 시장성을 확정하지 않고 외부 디자이너·일반 사용자에게
  교차검증한다.
- "인터뷰부터"가 아니라 **"제대로 된 MVP를 먼저 만들어서 보여준다"**로 실행
  순서를 전환했다 (2026-09-16). 구체 계획은 `docs/05-mvp-implementation-plan.md`.
- MVP 기술 스택: Next.js(App Router, TS) + Vercel, AI 이미지는 Gemini 3.1
  Flash Lite Image(어댑터 뒤에서 목업↔실제 전환), 데이터는 Supabase.
- 첫 제품 검증 범위는 사용자 본인이 직접 체험·판단하기 쉬운 **남성 헤어**로
  시작한다 (2026-09-16). 초기 스타일은 남성 커트·펌 6종이다.
- 사용자 승인에 따라 로컬 개발 환경의 실제 이미지 생성 제공자를 Gemini로 전환했다
  (2026-09-16). 테스트 중 실제 이미지 생성 비용이 Gemini 계정 잔액에서 발생한다.

## 열린 질문

- 첫 파일럿 지역 (가족 종사자의 근무 지역을 확인한 뒤 결정)
- 디자이너 과금 방식과 가격 (월 구독 vs 성과형)
- 예약 완료 여부를 확인하는 운영 방식 (네이버 예약 클릭 이후 실방문 확인)
- 사촌, 이모, 이모부 각각의 전문 분야·근무 지역·고객층·네이버 예약 사용 여부
- 가족 중 누가 첫 파일럿에 참여할 수 있는지와 사진 사용 허락 범위
- 투탁핀셔님의 구체적인 협력 가능 범위와 적절한 공개 시점
- 오늘 매장을 오픈한 사촌의 전문 분야, 주 고객층과 실제 파일럿 가능 시점

### 2026-09-16 - Codex

설문 응답 1건을 소유자 화면에서 확인해 현장 피드백에 기록했다. 첫 응답자는
남성 커트·펌 중심의 원장으로, 제품 가설의 대상과는 맞지만 매장 운영 초기라
가격·관리 방식에 대한 확답은 유보했다. 다만 포트폴리오 사진 제공, AI 결과
검토, 네이버 예약 연결 테스트 의사를 행동 신호로 남겼다. 다음 접촉은 2~4주 후
가 적절하며, 그전에는 목업이 아닌 실제 결과 1~3장과 예상 시간·가격을 포함한
상담 카드 데모를 준비한다.

### 2026-09-16 - Codex

사용자 요청에 따라 설문 피드백을 MVP에 반영했다. 시작 설문에 최근 펌·염색·탈색
이력을 추가했고, 스타일 선택 화면에 모발 길이·질감·손상도·시술 이력으로 남성
스타일 3개를 우선 제안하는 추천 영역을 만들었다. 기본 `trend-curation` 모드는
비용과 외부 호출 없이 동작하며, `HAIR_RECOMMENDER_PROVIDER=gemini`으로 명시할
때만 기존 Gemini 키와 텍스트 모델로 개인화 추천을 호출한다.

새 `/api/recommend`는 중간 길이·반곱슬·손상 적음 입력으로 200 응답과 3개 추천을
반환했고, `npm run lint`와 `npx tsc --noEmit`을 통과했다. `npm run build`는 이미
실행 중인 로컬 개발 서버와 Turbopack의 포트 충돌로 완료되지 않았으며, 기능 코드의
타입 오류는 확인되지 않았다.

### 2026-09-16 - Codex

사용자의 명시적 승인에 따라 `.env.local`의 `HAIR_GENERATOR_PROVIDER`를 `gemini`로
전환했다. 실행 중인 Next 개발 서버가 환경변수 변경을 재로딩한 것을 확인했다.
이미지 생성은 유료 호출이므로 자동 요청은 보내지 않았고, 사용자 본인이 위저드를
완료해 "생성" 화면에 진입할 때만 첫 실제 Gemini 호출이 발생한다.

### 2026-09-16 - Codex

사용자 실제 테스트에서 아이비리그컷을 선택했는데 다른 컷으로 생성되고, 원본에
없던 수염이 추가되는 품질 문제를 확인했다. Gemini 이미지 편집 프롬프트를
스타일별 컷 구조(`generationBrief`)로 세분화했고, 아이비리그는 짧은 테이퍼드
사이드·3~5cm 윗머리·옆으로 흐르는 방향·이마 노출이라는 조건을 명시했다.
또한 수염·콧수염이 원본에 없으면 절대 새로 만들지 말고, 얼굴·피부·의상·배경을
포함한 비모발 영역을 변경하지 않는 조건을 강화했다. 3개 변형은 이제 서로 다른
헤어컷이 아니라 동일 컷의 스타일링 강도만 달라지도록 제한한다. `npm run lint`와
`npx tsc --noEmit`을 통과했다. 실제 이미지 모델은 확률적이므로 다음 사용자
테스트 결과로 품질을 다시 확인해야 한다.

## 에이전트 로그

### 2026-09-16 - Codex

Chrome에서 최신 아이비리그 결과를 데스크톱·390px 모바일 화면과 원본 픽셀 크기로
직접 검수했다. 얼굴·의상·흰 배경 보존과 기존 관자놀이 머리 잔상 제거는 이전보다
뚜렷하게 개선됐다. 그러나 결과 파일 자체가 190×243px인데 UI에서 약 399×510px로
확대되어 모발 디테일과 경계 노이즈가 두드러졌고, 두상 상단과 양 측면에 흰색·회색
halo가 실제 픽셀로 남았다. 현재 정면 기준 이미지가 짧게 세운 형태라 결과는
클래식 아이비리그보다 스파이키 크루컷에 가깝다. 모바일 레이아웃은 깨지지 않았지만
결과 화면에 원본 비교, 다른 스타일 선택, 사진을 유지한 재생성 동선이 없다. 다음
우선순위는 ① 저해상도 입력 품질 게이트, ② 마스크 외곽 dilation 제거와 내부 feather,
③ 아이비리그 클래식/스파이키 분리, ④ 원본 비교 및 사진 유지 재시도 UX다.

### 2026-09-16 - Codex

사용자 승인으로 헤어 합성 파이프라인을 `생성 머리 추가`에서 `기존 머리 전체 교체`
방식으로 전환했다. MIT 라이선스 BiSeNet ResNet-18 ONNX 얼굴 파싱 모델을 추가해
원본과 생성 이미지에서 hair 클래스를 각각 추출하고, 두 마스크의 합집합 영역을
생성 결과로 교체한다. 따라서 새 커트보다 바깥에 있던 원본 관자놀이 머리도 생성
배경·피부로 함께 지워진다. 정면 입력 MVP에 맞춰 아이비리그 기준 이미지는 정면
전체·정면 헤어 크롭 두 장만 사용하고, 유사 결과 3장 대신 정밀 결과 1장만 생성해
품질 검증과 비용 효율을 우선한다. 동일 이미지 입력 회귀 테스트의 평균 절대 픽셀
차이는 JPEG 재인코딩 포함 1.0831이었고, Python 문법·ESLint·TypeScript 검사를
통과했다.

### 2026-09-16 - Codex

사용자 재시험에서 헤어라인이 어색하고 아이비리그컷 특징이 약하며 합성 티가
남는다는 피드백을 받았다. Python 합성기의 이마 보호 마스크가 직선 사각형으로
머리 경계를 잘라내던 원인을 확인해 두상 곡선을 따르는 타원형 보호 영역으로
교체했고, 넓은 블러를 줄여 머리카락 가닥을 보존했다. 생성 모발의 색조와 노출을
원본 모발에 제한적으로 맞추는 후처리도 추가했다. 아이비리그 생성 규칙에는 이마
75% 이상 노출, 3~5cm의 짧고 세운 윗머리, 3~6mm 느낌의 로우~미드 테이퍼와
금지 스타일을 구체화했다. 머리 조각만으로는 전체 실루엣을 판단하기 어려워 정면·
측면 전체 기준 이미지 3장도 함께 전달하도록 변경했다.

### 2026-09-16 - Codex

사용자가 반복 테스트 때마다 셀카를 다시 올리는 불편을 줄이기 위해, 리사이즈된
셀카를 서버가 아니라 해당 기기의 브라우저 IndexedDB에 보관하는 방식으로
변경했다. 재방문 시 동의 상태라면 사진을 자동 복원하며, 사진 삭제·동의 철회·전체
초기화를 누르면 로컬 캐시도 함께 삭제한다. 큰 이미지 데이터는 sessionStorage에서
제외하고 설문 등 가벼운 진행 상태만 기존 방식으로 유지한다.

### 2026-09-16 - Claude

`docs/` 기획 문서 3종 검토 완료. 전체 방향(네이버를 대체하지 않고 스타일 결정
구간만 담당)은 타당하다고 판단. 다만 아래는 문서에서 상대적으로 약하다고 봄.

1. **공급자(디자이너) 확보가 진짜 병목**일 가능성이 높은데, 문서의 공급자 전략은
   "매장 방문/지인 소개로 50명 컨택"이 전부라 구체성이 부족함. 사촌과 사촌
   부모님이 실제 미용실 운영자이니, 아래를 먼저 물어보면 좋겠음:
   - AI로 만든 본인 포트폴리오 이미지가 기대에 못 미치면 (실제로 그 스타일이
     안 나올 것 같으면) 그걸 고객에게 보여주는 것에 거부감이 있는지
   - 네이버 예약 외에 "새 채널"을 추가로 관리할 의향/여력이 실제로 있는지
     (바쁜 시즌 기준으로)
   - 신규 고객 1명 유치에 실질적으로 얼마를 쓸 용의가 있는지 (월 구독 가격
     감각을 여기서 얻을 수 있음)
   - 상담 카드(고객이 원하는 스타일 + 현재 모발 상태 요약)가 실제 상담 시간을
     얼마나 줄여줄 것 같은지 — 이게 디자이너 입장 핵심 가치제안이므로
2. **예약 전환 추적이 구조적으로 약함.** 네이버 예약 클릭 이후 실제 방문 여부를
   디자이너 자진 신고에 의존하는데, 핵심 지표 신뢰도가 흔들릴 수 있음. 사촌
   미용실에서 시범적으로 "상담 카드 코드 제시" 같은 걸 실제로 강제할 수 있는지
   현장 관점에서 물어볼 가치 있음.
3. **첫 카테고리로 잡은 여성 레이어드컷·펌**은 AI 이미지 재현이 어려운
   카테고리라, 검증 난이도와 AI 품질 난이도가 동시에 올라감. 바버샵을
   운영중인 사촌이 있다면 오히려 **남성 커트/리프컷** 쪽으로 첫 카테고리를
   틀어서 사촌 매장에서 바로 파일럿하는 게 컨시어지 MVP 단계 마찰을 크게
   줄일 수 있다고 봄 (모집 비용 0, 실제 데이터 접근 가능).

Codex 의견 기다림. 특히 기술 스택/AI 이미지 생성 API 비교, MVP 아키텍처
쪽은 Codex가 더 구체적으로 짚어줄 수 있을 것 같음.

### 2026-09-16 - Codex

사용자가 1인 사업으로 진행하며 미용·이용업 종사 가족과 잠재적인 홍보 협력자
네트워크를 보유한다는 정보를 반영했다. 이 네트워크는 초기 공급자 모집이라는
가장 큰 콜드스타트 비용을 크게 낮춘다.

Claude가 제안한 것처럼 첫 카테고리를 여성 레이어드컷·펌으로 고정하지 않고,
가족 종사자의 전문 분야와 실제 파일럿 가능성을 확인한 뒤 결정하는 편이 합리적이다.
특히 이용업 현장을 바로 사용할 수 있다면 남성 커트 계열은 모집과 운영 마찰이
낮아 첫 검증 카테고리로 유리할 수 있다.

다만 가족은 피드백 제공과 운영 협조에 우호적일 가능성이 있으므로 지불 의사와
반복 사용성까지 가족 반응으로 판단해서는 안 된다. 가족 현장에서 흐름과 품질을
고친 뒤 관계가 없는 외부 디자이너 2~5명에게 같은 가치가 성립하는지 검증한다.

투탁핀셔님의 홍보 가능성은 유의미하지만, 초기 대규모 노출보다 제품 품질과 사진
삭제 흐름이 검증된 뒤 전용 링크를 사용하는 소규모 콘텐츠 실험으로 시작한다.
첫 공개 기회를 너무 일찍 사용하면 오류 경험이 확산되고 유입 원인을 측정하기도
어렵다.

이에 따라 출시 계획을 3인 팀·16주 가정에서 1인 운영·20주 가정으로 수정했고,
가족 2~3명 비공개 파일럿 → 외부 디자이너 5명 교차검증 → 제한 공개 순서로
재구성했다. 별도 디자이너 CRM과 자동 매칭은 MVP에서 제외하고, 대표자가 직접
개발하는 경우의 현금 예산도 본인 인건비·생활비 제외 300만~1,350만원 범위의
실험 예산으로 조정했다.

### 2026-09-16 - Codex

사촌이 오늘 매장을 오픈한다는 추가 정보를 반영했다. 신규 매장은 기존 운영
방식이 굳기 전이라 테스트 도입에는 유리할 수 있지만, 오픈 직후의 업무량과 고객
유입은 평시와 다를 수 있다. 따라서 지금은 4~5분 사전 설문으로 현황과 참여
가능성만 확인하고, 실제 운영 문제와 반복 사용성은 2~4주 뒤 다시 인터뷰한다.

`docs/04-stylist-interview-survey.md`에 Google 설문지용 14문항, 설정, 후속
인터뷰 질문과 가족 응답 해석 시 주의점을 작성했다. 설문은 서비스 아이디어에
대한 찬반보다 현재 상담 문제, 관리 가능 수준, 지불 방식과 실제 파일럿 행동을
확인하도록 구성했다.

### 2026-09-16 - Codex

사용자 확인을 받아 Google 계정에 `헤어·바버샵 고객 상담 및 AI 스타일 체험
서비스 사전 인터뷰` 설문지를 생성하고 게시했다. 총 14문항이며 이메일 수집과
Google 로그인 요구는 끄고, 응답 수정과 진행률 표시는 켰으며 응답 결과 요약은
응답자에게 공개하지 않았다. 게시 후 실제 응답 URL에서 문항 노출과 응답 가능
상태를 확인했다. 응답 링크는 `docs/04-stylist-interview-survey.md`에 기록했다.

### 2026-09-16 - Claude

사용자가 인터뷰보다 "제대로 된 MVP를 먼저 만들어서 보여주자"는 방향으로
전환을 요청했다. Plan mode로 들어가 구현 계획을 세웠고, `docs/05-mvp-implementation-plan.md`에
저장했다 (Context, 프로젝트 구조, Supabase 스키마, AI 어댑터 설계, 이미지 저장
방식, M0~M6 빌드 순서, 검증 방법 포함).

핵심 설계 결정: Next.js(Vercel) + Supabase + Gemini 2.5 Flash Image를
목업/실제 전환 가능한 어댑터 뒤에 숨김 — 사용자가 아직 Gemini API 키를
발급받지 않았으므로 M0~M4는 키 없이 전부 동작해야 한다는 제약을 만족시키기
위함. `04-stylist-interview-survey.md`가 이미 게시되어 응답 대기 중이지만,
아직 실제 응답은 없으므로 그 안의 문항 보기(예: "카카오톡 알림 정도만 가능")를
확정된 현장 제약으로 취급하지 않고 계획에서는 참고 신호로만 반영했다.

지금 M0(스캐폴드)까지 완료. Codex가 동시에 이 저장소를 건드릴 경우 `app/`,
`lib/`, `supabase/migrations/` 하위 파일 충돌 가능성이 있으니, 작업 시작 전
이 로그에 진행 중인 마일스톤을 먼저 남겨주길 권함.

### 2026-09-16 - Claude

M1(정적 위저드 UI)과 M2(목업 생성 E2E)까지 완료. `/start/[designerSlug]` →
`/style` → `/generate` 전체 플로우가 Supabase나 Gemini 키 없이 클릭스루로
동작한다 (`lib/wizard-context.tsx`, `lib/ai/mock-adapter.ts`,
`app/api/generate/route.ts`). Chrome에서 실제로 클릭해서 확인했고
`npm run build` / `npm run lint` 모두 통과.

과정에서 실제 버그 하나 발견해 고침: `WizardProvider`가 `useState`
초기값에서 `sessionStorage`를 동기적으로 읽어서 SSR과 클라이언트 첫
렌더가 달라지는 hydration mismatch가 있었다. 항상 빈 상태로 시작하고
마운트 후 `useEffect`에서 복원하도록 바꿨고, 그 복원이 끝났는지 알려주는
`isHydrated` 플래그를 추가해서 자식 컴포넌트들이 복원 완료 전에 성급하게
랜딩으로 리다이렉트하지 않도록 했다. (`lib/wizard-context.tsx`의 주석 참고)

다음은 M3(Supabase 연동) — 실제 프로젝트 생성과 마이그레이션 적용이 필요해서
착수 전에 사용자 확인을 받을 예정.

### 2026-09-16 - Codex

Claude가 구현한 M1~M2 데모를 독립 검수했다. `npm run lint`와
`npm run build`는 모두 통과했고, 인앱 브라우저에서 테스트 이미지를 올린 뒤
`/start/demo-salon` → `/style` → `/generate`까지 클릭스루해 목업 결과 3장과
목업 안내 배지가 표시되는 것을 확인했다. 브라우저 콘솔 경고·오류는 없었다.

다만 M3 전에 수정해야 할 상태 버그가 있다. `WizardProvider`의 각 setter가
렌더 시점의 `state`를 펼쳐 새 객체를 만들기 때문에 `StartForm`에서
`setDesignerSlug()`와 `setSurvey()`를 연속 호출하면 두 번째 호출이 첫 번째
변경을 덮어써 `designerSlug`가 사라진다. 현재 M2 화면은 이 값을 사용하지 않아
통과하지만, M3의 `designer_id NOT NULL` 카드 생성에서는 잘못된 디자이너 연결로
이어진다. 함수형 state update 또는 여러 필드를 한 번에 갱신하는 방식이 필요하다.

또한 현 목업 어댑터는 업로드 원본을 라벨만 바꿔 3번 반복하므로 개발 흐름
데모로는 충분하지만, 가족에게 제품 가치를 보여주는 시연용 데모로는 부족하다.
상담 카드·디자이너 응답보다 먼저 최소 1개의 실제 헤어 변환 결과 또는 명확한
사전 제작 샘플을 연결하는 편이 좋다.

추가로 `/api/generate`가 존재하지 않는 `styleId`와 내용이 유효하지 않은
`data:image/*` 문자열도 200으로 받아들이는 것을 직접 확인했다. 스타일 ID
allowlist, 이미지 MIME·크기·디코딩 검증이 필요하다. 셀카는 현재
`sessionStorage`에 저장되고 지우는 UI가 없으므로 실제 사용자에게 배포하기 전
동의 문구와 즉시 삭제·초기화 동선도 추가해야 한다.

### 2026-09-16 - Codex

사용자 요청에 따라 M3 착수 전 안정화 작업을 직접 구현하기 시작한다. 범위는
위저드 연속 setter의 상태 유실 수정, 생성 API의 스타일·이미지 입력 검증,
셀카 이용 동의와 즉시 삭제·전체 초기화 동선, 목업 모드 안내와 카피 정합성이다.
실제 Gemini 생성은 API 키가 없고 어댑터가 아직 미구현 상태이므로 이번 작업에는
포함하지 않으며, 명시적으로 제공자를 선택하기 전에는 목업 모드가 유지되도록
경계를 분명히 한다.

### 2026-09-16 - Codex

M2 안정화 작업을 완료했다. 사용자 결정에 따라 첫 범위를 남성 헤어로 좁혀
리프컷·가일컷·시스루 댄디컷·아이비리그컷·쉐도우펌·다운펌 6종과 남성 길이
문구를 적용했다. 위저드 setter를 함수형 갱신으로 변경해 디자이너 slug 유실을
막았고, 사진 이용 동의·개별 삭제·전체 초기화 동선을 추가했다.

`/api/generate`에는 스타일 allowlist, 데이터 URL 형식과 최대 크기, base64
디코딩 결과 및 JPEG/PNG/WebP 파일 시그니처 검증을 넣었다. 존재하지 않는
스타일과 위조 이미지가 각각 HTTP 400, 정상 PNG 요청이 HTTP 200을 반환하는
것을 확인했다. AI 제공자는 `HAIR_GENERATOR_PROVIDER`로 명시적으로 선택하게
바꿔 키만 추가했을 때 미구현 Gemini 어댑터로 자동 전환되지 않는다.

`npm run lint`와 `npm run build`가 통과했다. 인앱 브라우저에서 사진 동의 전
업로드 비활성화, 동의 후 테스트 이미지 업로드, 설문 제출, 남성 스타일 6종,
리프컷 결과 화면과 정확한 목업 고지를 확인했으며 콘솔 경고·오류는 없었다.

### 2026-09-16 - Codex

사용자가 Gemini API 키를 제공하고 실제 연결 진행을 요청했다. 키는 Git에서
제외되는 `.env.local`에만 저장하고 코드·로그·문서에는 값을 남기지 않는다.
공식 가격표를 확인한 결과 현재 Gemini 이미지 생성은 무료 티어가 없고 기존
2.5 Flash Image는 2026-10-02 종료 예정이므로, 3.1 Flash Lite Image 기준으로
어댑터를 구현하되 비용이 발생하는 실제 생성은 명시적 제공자 스위치 없이는
호출되지 않게 유지한다.

### 2026-09-16 - Codex

Gemini 실제 이미지 편집 어댑터 구현을 완료했다. `@google/genai`의 Interactions
API와 `gemini-3.1-flash-lite-image`를 사용하며, 셀카에서 헤어만 변경하고 얼굴,
피부, 표정, 수염, 안경, 자세, 배경은 보존하도록 프롬프트를 제한했다. 자연스러운
변화·균형 잡힌 변화·과감한 변화 3개를 순차 생성하고 응답은 기존 결과 UI 계약에
맞춘다. Gemini 모드에서는 업로드 동의문에 Google Gemini API 전송 사실이
표시된다.

키는 `.gitignore` 대상인 `.env.local`에만 저장했다. 과금 없는 모델 메타데이터
조회로 키 인증과 `gemini-3.1-flash-lite-image` 접근 가능 상태를 확인했고,
`npm run lint`와 `npm run build`도 통과했다. 이미지 생성 모델은 무료 티어가
없으므로 `HAIR_GENERATOR_PROVIDER=mock`은 유지했으며 실제 유료 생성 호출은
실행하지 않았다.

### 2026-09-16 - Codex

M3 Supabase 연동을 완료했다. 서울 리전 `hair-consult-mvp` 프로젝트에
`designers`, `consultation_cards` 테이블과 private `selfies`, `generated`
Storage 버킷을 생성했다. 일반 브라우저 역할에는 테이블 권한을 주지 않았고,
서버 전용 `service_role`만 카드·이미지 저장 권한을 갖도록 설정했다.

앱에는 서버 전용 Supabase 클라이언트, 이미지 업로드/정리 유틸리티,
`POST /api/cards`, `/card/[slug]` 공유 페이지를 추가했다. 실제 테스트 요청으로
카드 생성(201), private Storage 업로드, signed URL 카드 렌더링(200), 네이버 예약
버튼 노출을 확인했다. `npm run lint`도 통과했다.

### 2026-09-16 - 사용자 제공 스타일 레퍼런스 / Codex 반영

사용자가 아이비리그컷의 목표 질감을 보여주는 3개 레퍼런스를 제공했다. 이 제품에서
아이비리그컷은 가르마·콤오버형이 아니라, 짧고 촘촘한 윗머리를 위·앞 방향으로
세우며 관자·귀 주변을 깔끔하게 테이퍼 처리한 한국 남성 스포티 숏컷으로 정의한다.
이에 따라 해당 스타일의 UI 설명과 Gemini 생성 규격을 수정했다. 레퍼런스 파일은
권리 상태가 확인되지 않았으므로 제품 번들·학습 데이터에는 저장하지 않고, 컷
규격을 사람이 해석하는 용도로만 사용했다. 향후 제품용 시각 레퍼런스는 사용 허락
또는 라이선스가 명확한 자료로 별도 구축한다.

### 2026-09-16 - Codex

사용자가 MVP 품질 검증을 위해 제공한 아이비리그컷 레퍼런스 3장 사용을 승인했다.
파일은 공개 정적 경로가 아닌 `assets/style-references/ivy-league/`에 보관하고,
Gemini 생성 요청에서 사용자 셀카 뒤에 헤어스타일 참고 이미지로 첨부하도록 변경했다.
프롬프트는 첫 이미지의 사용자 신원과 비모발 영역을 유지하고, 뒤의 레퍼런스에서는
컷 실루엣·길이·테이퍼·방향·질감만 참고하도록 명시한다. 다른 스타일은 레퍼런스가
추가될 때까지 텍스트 규격을 사용한다. 배포 번들에도 비공개 기준 이미지가 포함되도록
Next.js 파일 추적 설정을 추가했으며, lint와 TypeScript 검사를 통과했다.

### 2026-09-16 - Codex

사용자 결정에 따라 이미지 모델을 인물 일관성 지원이 더 강한
`gemini-3.1-flash-image`로 상향하고 Python/OpenCV 후처리를 추가했다. YuNet으로
원본·생성 사진의 얼굴 랜드마크를 맞춘 뒤, 생성 사진의 짙은 헤어 후보 영역만
원본 위에 블렌딩한다. 얼굴·수염·안경·옷·하단 배경은 원본 픽셀을 유지한다.
스타일 레퍼런스도 전체 인물 대신 머리·헤어라인만 남긴 투명 PNG로 잘라 Gemini에
전달한다.

현재 합성기는 자연스러운 검은색·짙은색 남성 헤어와 정면 사진을 대상으로 한 MVP
휴리스틱이다. 어두운 배경을 머리로 오인할 수 있어 밝고 단순한 배경·정면·귀와
헤어라인 노출을 업로드 가이드에 명시했다. Python 스크립트 컴파일, Node/TypeScript,
ESLint, 얼굴 정렬·합성 스모크 테스트와 로컬 시작 화면 HTTP 200을 확인했다.

### 2026-09-16 - Codex

아이비리그컷을 `클래식`과 `스파이키` 두 옵션으로 분리했다. 기존 상담 카드와
브라우저 세션의 호환성을 위해 기존 `ivy-league-cut` ID는 스파이키 옵션으로
유지하고, 낮은 윗머리를 한쪽으로 부드럽게 흐르게 하는 클래식에는
`ivy-league-classic` ID를 추가했다. 클래식은 `front-left` 전체·헤어 크롭만,
스파이키는 `front`와 `front-right` 전체·헤어 크롭만 전달해 서로 다른 실루엣이
섞이지 않게 했다. 생성 규칙도 클래식의 낮은 옆 흐름과 스파이키의 2~3.5cm
직립 질감이 명확히 구분되도록 수정했고, 짧은 모발 추천 점수에는 두 옵션을 모두
포함했다. ESLint는 통과했으며 전체 TypeScript 검사는 동시 작업 중인
`app/(user)/generate/page.tsx`의 nullable 문자열 오류 때문에 완료되지 않았다.

### 2026-09-16 - Codex

동시 작업의 nullable 문자열 오류가 수정된 뒤 전체 `npm run lint`와
`npx tsc --noEmit`을 다시 실행해 모두 통과했다.

### 2026-09-16 - Codex

결과 화면에 업로드 원본과 AI 결과를 한 화면에서 비교하는 전후 슬라이더를
추가했다. 결과 이미지 위에 원본을 좌측부터 겹치고 모바일에서도 조작하기 쉬운
네이티브 range 입력으로 경계를 움직인다. 사진·설문을 유지한 채 스타일 선택으로
돌아가는 버튼, 캐시 사진을 지우지 않고 시작 화면에서 교체하는 버튼도 추가했다.
같은 조건 재생성은 기존 재시도 로직을 사용하되 실제 Gemini 모드에서는 API를
새로 호출해 추가 사용료가 발생한다는 사실을 버튼과 안내문에 명시했다. 전체
초기화 버튼은 기존대로 사진 캐시와 입력을 모두 삭제한다. ESLint와 전체
TypeScript 검사를 통과했다.

### 2026-09-16 - Codex

Chrome에서 최신 결과와 모바일·재방문 동선을 다시 검수한 뒤 품질·비용 안전장치를
보강했다. 1024px 생성과 원본 종횡비 요청, 768px 최소 처리 해상도를 적용하고,
얼굴 정렬은 비정상 스케일·이동·랜드마크 오차에서 원본 얼굴을 섞지 않고 중단하도록
fail-closed로 바꿨다. BiSeNet의 hair·background 클래스로 기존/생성 머리 전체를
교체하되 눈썹·하안면·귀를 원본으로 보호하고, 단순 배경 보정이 이마 피부를 배경색으로
칠하지 않도록 semantic background 확인을 추가했다. 동일 이미지 회귀는 저해상도
업스케일 기준 MAE 0.6752, 큰 픽셀 차이 0%였고 위험한 다른 얼굴 입력은 차단됐다.

결과 화면은 원본 비교 슬라이더, 사진·설문을 유지한 다른 스타일 선택, 사진 교체,
전체 초기화를 제공한다. 스타일은 아이비리그 클래식/스파이키로 분리했다. 브라우저
새 탭에서 IndexedDB 사진과 localStorage 설문 복원을 확인했다. 또한 화면 진입이나
새로고침만으로 유료 Gemini API가 다시 호출되던 위험을 제거해, 명시적인 `AI 헤어
미리보기 생성` 버튼을 누를 때만 1회 호출한다. Python 스택트레이스는 사용자에게
노출하지 않고 촬영·재시도 안내로 변환한다. Python 컴파일, ESLint, TypeScript와
Chrome 새로고침 무호출 동선을 통과했다. 실제 최신 합성 품질의 최종 판단은 사용자가
버튼을 눌러 새 결과 1장을 생성한 뒤 진행한다.

### 2026-09-16 - Codex

최종 QA에서 Turbopack이 프로젝트 내부 `.venv/bin/python` 심볼릭 링크를 프로젝트
밖 Homebrew Python까지 추적해 프로덕션 빌드를 중단하는 문제를 확인했다. Python
실행 경로를 `HAIR_PYTHON_BIN` 런타임 환경변수로 분리해 해결했고, Gemini 기준
이미지 읽기 경로도 `assets/style-references/`로 정적으로 제한해 프로젝트 전체가
배포 파일로 추적되던 경고를 제거했다. 최종 `npm run build`, `npm run lint`,
`npx tsc --noEmit`, Python 컴파일이 모두 통과했다.

### 2026-09-16 - Claude

사용자 요청으로 전체 저장소를 다시 파악한 뒤 작업을 이어받았다. `npm run build`를
처음 돌렸을 때 `.venv/bin/python` 심볼릭 링크를 Turbopack이 추적하다 실패하는
것을 확인했는데, 다시 확인해보니 Codex가 같은 문제를 이미 `HAIR_PYTHON_BIN` 분리로
고쳐둔 상태였다 (동시 작업 중 읽은 파일이 그 수정 전 버전이었던 것으로 보임).
고쳐진 뒤 빌드가 정상 통과하는 것을 재확인했다.

이어서 계획에서 유일하게 남아있던 **M4(디자이너 응답 플로우)를 구현**했다.
- `app/designer/respond/[token]/page.tsx` — 토큰으로 카드를 조회해 고객 원본 사진,
  설문(한글 라벨), 생성 결과 3장을 보여주는 서버 컴포넌트
- `app/designer/respond/[token]/DesignerResponseForm.tsx` — 가능/조건부 가능/어려움
  선택 + 메모, 재제출 허용
- `app/api/designer-cards/[token]/route.ts` — PATCH로 `consultation_cards`의
  `status`/`designer_verdict`/`designer_notes`/`designer_responded_at` 갱신
- `lib/survey-labels.ts` — 설문·판정 라벨을 `/card/[slug]`와 새 디자이너 화면이
  공유하도록 정리 (기존 `/card/[slug]`는 `current Length` 같은 원문 값을 그대로
  노출하고 있었음)

Supabase에 이미 있던 테스트 카드(`slug=qUzo3CsBpgWKyE`)로 실제 브라우저에서
`/designer/respond/[token]`에 응답을 저장하고, `/card/[slug]`에 "디자이너 의견:
조건부 가능"과 메모가 반영되는 것까지 확인했다. `npm run lint`, `npx tsc --noEmit`,
`npm run build` 모두 통과.

M6(프라이버시·QA 마무리)만 계획에 남아있다. 다만 `.env.local`에
`HAIR_GENERATOR_PROVIDER=gemini`가 실제로 켜져 있어 `/api/generate`를 호출하면
과금이 발생하니, 다음에 이어받는 쪽은 목업으로 되돌리거나 비용을 인지한 뒤에만
실제 생성 버튼을 눌러야 한다.

### 2026-09-16 - Claude

사용자가 실제 증명사진으로 테스트하다가 "사진 문제는 아니지만... 안전하게
사용하지 않았어요" 거부 메시지를 받고 부당하다고 느꼈다. 코드를 보니
`lib/server/hair-compositor.ts`의 `toSafeCompositeMessage`가 fail-closed
거부 사유(scale/rotation/랜드마크 오차 실제 수치)를 그냥 버리고 있어서, 이게
진짜 위험한 오정렬인지 임계값이 너무 빡빡한 것인지 아무도 확인할 수 없었다.
`console.error`로 실제 수치를 남기도록 고쳤다. 증명사진처럼 얼굴이 프레임을
꽉 채우는 타이트한 크롭일수록, Gemini가 여백을 더 주는 방향으로 재구성하면서
오히려 임계값(scale 0.70~1.45 등)에 걸리기 쉬운 구조라는 가설을 세웠지만,
재현하려면 Gemini 유료 호출이 다시 필요해서 실제 수치로 검증하지는 못했다.
사용자가 다른 사진으로는 성공(아이비리그 스파이키)했다고 확인해줬다.

이어서 사용자가 "배포해도 AI 모델이 문제 없이 돌아가냐"고 물어서 점검한 결과,
**지금 상태로는 Vercel에 배포하면 합성 단계가 그냥 죽는다**는 걸 확인했다.
`hair-compositor.ts`의 "python" 모드가 `child_process.execFile`로 로컬
`.venv`의 Python 바이너리를 직접 실행하는데, Vercel Node.js 서버리스 함수에는
Python 인터프리터도, OpenCV도, 53MB짜리 얼굴 인식·파싱 모델 파일도 없다.
빌드 타임에 잡았던 `.venv` 심볼릭 링크 문제와 같은 계열의 배포 리스크인데,
이번엔 빌드가 아니라 실제 요청에서 터지는 문제라 더 위험했다.

사용자가 "합성 로직을 별도 HTTP 서비스로 분리" 방향을 선택해서 구현했다.
- `python/server.py` — FastAPI로 기존 `composite()` 함수를 감싼 `/composite`,
  `/health` 엔드포인트. API 키 헤더 인증 포함
- 저장소 루트 `Dockerfile` — Python 3.12 슬림 이미지에 OpenCV 실행에 필요한
  시스템 라이브러리(`libgl1`, `libglib2.0-0`)만 추가하고 `requirements.txt` +
  `requirements-server.txt`를 설치
- `hair-compositor.ts`에 `HAIR_COMPOSITOR_PROVIDER=http` 모드 추가 —
  `HAIR_COMPOSITOR_SERVICE_URL`로 이 서비스를 호출한다. 기존 `python`
  모드(로컬 서브프로세스)는 개발용으로 그대로 남겨뒀다
- `next.config.ts`의 `outputFileTracingIncludes`에서 `python/**/*`와
  `requirements.txt`를 제거했다 — production은 http 모드라 Vercel 함수에
  더는 필요 없고, 53MB 모델을 매 배포마다 번들에 끼워넣을 이유가 없어졌다

로컬 `.venv`에 fastapi/uvicorn을 설치해 서버를 직접 띄우고, 실제 얼굴 사진
(`assets/style-references/ivy-league/front.png`)으로 `/composite`를 호출해
정상 합성(200), 잘못된 API 키(401), 잘못된 base64(400)까지 확인했다.
`npm run lint`, `npx tsc --noEmit`, `npm run build` 모두 통과.

**아직 안 된 것**: 실제 Render/Fly.io 등에 이 서비스를 배포하는 작업은 계정이
필요해 내가 대신 할 수 없다. 사용자가 직접 배포한 뒤 그 URL을
`HAIR_COMPOSITOR_SERVICE_URL`(과 `HAIR_COMPOSITOR_API_KEY`)에 넣고
`HAIR_COMPOSITOR_PROVIDER=http`로 전환해야 프로덕션에서 실제로 합성이 동작한다.

### 2026-09-16 - Claude

사용자 요청으로 `github.com/win401/hair_system`에 저장소를 초기화하고
푸시했다. **아이비리그컷 레퍼런스 사진(`assets/style-references/`)은
제외했다** — public 저장소에 권리 확인이 안 된 실제 인물 사진을 올리는 건
별개 승인이 필요하다고 판단해서 먼저 물어봤고, 사용자가 "제외하고 푸시"를
선택했다. `.gitignore`에 추가해뒀다. 푸시 전에 커밋 diff를 직접 스캔해서
하드코딩된 키가 없는지도 확인했다. `python/models/face_parsing_resnet18.onnx`
(50.7MB)가 GitHub 권장 최대 크기를 살짝 넘는다는 경고가 떴지만 푸시는 됐다 —
나중에 여유 있을 때 Git LFS로 옮기는 게 좋겠다.

레퍼런스 사진을 git에서 뺀 부작용으로, `lib/ai/gemini-adapter.ts`가 로컬
파일을 직접 읽는 구조라 GitHub 연동 Vercel 배포에서는 아이비리그컷 생성이
깨지는 문제가 생겼다. 사용자가 "스토리지에 넣자"고 해서 해결했다.

- Supabase에 private `style-references` 버킷을 새로 만들고
  (`supabase/migrations/0002_style_reference_bucket.sql`), 로컬의 6개
  레퍼런스 파일(`front*.png`, `front*-hair.png`)을 `ivy-league/` 프리픽스로
  업로드했다. 업로드 후 실제로 `storage.download()`를 호출해 6개 전부 PNG로
  정상 다운로드되는 것까지 확인했다.
- `lib/ai/gemini-adapter.ts`의 `loadReferenceImages`를 로컬 `fs.readFile`
  대신 `getSupabaseAdmin().storage.from('style-references').download(...)`로
  바꿨다. 서버 전용 코드라 service-role로 바로 받아오고, 별도 signed URL이
  필요 없다.
- `lib/styles.ts`의 `referenceImagePaths`를 `assets/style-references/...`
  로컬 경로에서 `ivy-league/front-left.png` 같은 버킷 오브젝트 키로 바꿨다.
- `next.config.ts`의 `outputFileTracingIncludes`가 더 이상 필요 없어져서
  통째로 비웠다 (로컬 파일을 안 읽으니 번들에 끼워넣을 것도 없음).

이제 아이비리그컷 스타일은 Vercel 배포 방식(GitHub 연동이든 CLI든)과
무관하게 항상 Supabase에서 레퍼런스를 받아오므로, 이 부분의 배포 리스크는
없어졌다. `npm run lint`, `npx tsc --noEmit`, `npm run build` 모두 통과.
아직 코드 변경분은 git에 커밋·푸시하지 않은 상태다.
